---
import { Image } from 'astro:assets';
import icon from './sucklessdb-logo.png';

const { size } = Astro.props;

const finalSize = size || 32;
---

<Image src={icon} alt="SucklessDB Logo" width={finalSize} height={finalSize} />
